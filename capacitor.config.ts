import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wahoofitness.mapboxtest',
  appName: 'mapbox-test',
  webDir: 'dist'
};

export default config;
